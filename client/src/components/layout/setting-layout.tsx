import React from 'react';
import {Outlet} from "react-router-dom";

const Setting = () => {
    return (
        <div className=''>
            <Outlet />
        </div>
    )
};

export default Setting;